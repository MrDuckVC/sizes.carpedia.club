services:
  nginx:
    restart: unless-stopped
    image: nginx:alpine
    volumes:
      - ./docker/nginx/conf.d:/etc/nginx/conf.d:ro
    ports:
      - 8000:80

  web:
    extra_hosts:
    - "host.docker.internal:host-gateway"
